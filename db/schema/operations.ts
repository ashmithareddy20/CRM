import { sql } from "drizzle-orm";
import { index, integer, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
import { tenantColumns } from "./common";
export const operationalTasks = sqliteTable("crm_tasks", { id: text("id").primaryKey(), ...tenantColumns(), leadId: text("lead_id"), assigneeMembershipId: text("assignee_membership_id"), title: text("title").notNull(), dueAt: integer("due_at", { mode: "timestamp_ms" }).notNull(), status: text("status").notNull().default("open"), priority: text("priority").notNull().default("normal") }, (t) => [index("crm_tasks_queue_idx").on(t.tenantId, t.assigneeMembershipId, t.status, t.dueAt)]);
export const slaClocks = sqliteTable("crm_sla_clocks", { id: text("id").primaryKey(), ...tenantColumns(), leadId: text("lead_id"), taskId: text("task_id"), policyKey: text("policy_key").notNull(), dueAt: integer("due_at", { mode: "timestamp_ms" }).notNull(), status: text("status").notNull().default("running") }, (t) => [index("crm_sla_due_idx").on(t.tenantId, t.status, t.dueAt)]);
export const businessCalendars = sqliteTable("crm_business_calendars", { id: text("id").primaryKey(), ...tenantColumns(), branchId: text("branch_id"), timezone: text("timezone").notNull(), scheduleJson: text("schedule_json").notNull() });
export const escalationRules = sqliteTable("crm_escalation_rules", { id: text("id").primaryKey(), ...tenantColumns(), key: text("key").notNull(), thresholdSeconds: integer("threshold_seconds").notNull(), definitionJson: text("definition_json").notNull() }, (t) => [uniqueIndex("crm_escalation_rule_uq").on(t.tenantId, t.key)]);
export const escalationEvents = sqliteTable("crm_escalation_events", { id: text("id").primaryKey(), ...tenantColumns(), taskId: text("task_id"), ruleId: text("rule_id").notNull(), threshold: integer("threshold").notNull(), occurredAt: integer("occurred_at", { mode: "timestamp_ms" }).notNull() }, (t) => [uniqueIndex("crm_escalation_once_uq").on(t.tenantId, t.taskId, t.ruleId, t.threshold)]);
export const notifications = sqliteTable("crm_notifications", { id: text("id").primaryKey(), ...tenantColumns(), membershipId: text("membership_id").notNull(), type: text("type").notNull(), payloadCiphertext: text("payload_ciphertext"), readAt: integer("read_at", { mode: "timestamp_ms" }) }, (t) => [index("crm_notifications_idx").on(t.tenantId, t.membershipId, t.readAt)]);
export const doctors = sqliteTable("crm_doctors", { id: text("id").primaryKey(), ...tenantColumns(), branchId: text("branch_id").notNull(), membershipId: text("membership_id"), name: text("name").notNull(), active: integer("active", { mode: "boolean" }).notNull().default(true) }, (t) => [uniqueIndex("crm_doctors_tenant_id_uq").on(t.tenantId, t.id)]);
export const doctorAvailability = sqliteTable("crm_doctor_availability", { id: text("id").primaryKey(), ...tenantColumns(), doctorId: text("doctor_id").notNull(), startsAt: integer("starts_at", { mode: "timestamp_ms" }).notNull(), endsAt: integer("ends_at", { mode: "timestamp_ms" }).notNull(), status: text("status").notNull().default("available") }, (t) => [index("crm_doctor_availability_idx").on(t.tenantId, t.doctorId, t.startsAt)]);
export const appointmentSeries = sqliteTable("crm_appointment_series", { id: text("id").primaryKey(), ...tenantColumns(), leadId: text("lead_id").notNull(), doctorId: text("doctor_id"), branchId: text("branch_id").notNull(), status: text("status").notNull().default("active") }, (t) => [index("crm_appointment_series_lead_idx").on(t.tenantId, t.leadId)]);
export const appointmentEvents = sqliteTable("crm_appointments", { id: text("id").primaryKey(), ...tenantColumns(), seriesId: text("series_id").notNull(), leadId: text("lead_id").notNull(), doctorId: text("doctor_id").notNull(), branchId: text("branch_id").notNull(), startsAt: integer("starts_at", { mode: "timestamp_ms" }).notNull(), endsAt: integer("ends_at", { mode: "timestamp_ms" }).notNull(), status: text("status").notNull().default("suggested") }, (t) => [uniqueIndex("crm_appointments_tenant_id_uq").on(t.tenantId, t.id), index("crm_appointments_doctor_idx").on(t.tenantId, t.doctorId, t.startsAt)]);
export const slotReservations = sqliteTable("crm_slot_reservations", { id: text("id").primaryKey(), ...tenantColumns(), appointmentId: text("appointment_id").notNull(), doctorId: text("doctor_id").notNull(), branchId: text("branch_id").notNull(), slotStartAt: integer("slot_start_at", { mode: "timestamp_ms" }).notNull(), status: text("status").notNull().default("active") }, (t) => [uniqueIndex("crm_slot_active_occupancy_uq").on(t.tenantId, t.doctorId, t.branchId, t.slotStartAt).where(sql`${t.status} = 'active'`), index("crm_slot_lookup_idx").on(t.tenantId, t.doctorId, t.branchId, t.slotStartAt, t.status)]);
