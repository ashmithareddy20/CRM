import { index, integer, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
import { tenantColumns } from "./common";
export const templates = sqliteTable("crm_templates", { id: text("id").primaryKey(), ...tenantColumns(), key: text("key").notNull(), purpose: text("purpose").notNull(), channel: text("channel").notNull(), status: text("status").notNull().default("draft") }, (t) => [uniqueIndex("crm_templates_key_uq").on(t.tenantId, t.key)]);
export const templateVersions = sqliteTable("crm_template_versions", { id: text("id").primaryKey(), ...tenantColumns(), templateId: text("template_id").notNull(), versionNumber: integer("version_number").notNull(), contentCiphertext: text("content_ciphertext").notNull(), contentHash: text("content_hash").notNull(), approvedAt: integer("approved_at", { mode: "timestamp_ms" }) }, (t) => [uniqueIndex("crm_template_version_uq").on(t.tenantId, t.templateId, t.versionNumber)]);
export const templateApprovals = sqliteTable("crm_template_approvals", { id: text("id").primaryKey(), ...tenantColumns(), templateVersionId: text("template_version_id").notNull(), approvedByMembershipId: text("approved_by_membership_id").notNull(), decision: text("decision").notNull(), decidedAt: integer("decided_at", { mode: "timestamp_ms" }).notNull() });
export const contentAssets = sqliteTable("crm_content_assets", { id: text("id").primaryKey(), ...tenantColumns(), objectKey: text("object_key").notNull(), contentHash: text("content_hash").notNull(), mediaType: text("media_type").notNull(), status: text("status").notNull().default("quarantined") }, (t) => [uniqueIndex("crm_content_asset_uq").on(t.tenantId, t.objectKey)]);
export const consentEvents = sqliteTable("crm_consent_events", { id: text("id").primaryKey(), ...tenantColumns(), contactId: text("contact_id").notNull(), purpose: text("purpose").notNull(), channel: text("channel"), state: text("state").notNull(), evidenceId: text("evidence_id"), occurredAt: integer("occurred_at", { mode: "timestamp_ms" }).notNull(), expiresAt: integer("expires_at", { mode: "timestamp_ms" }) }, (t) => [index("crm_consent_contact_idx").on(t.tenantId, t.contactId, t.purpose, t.channel, t.occurredAt)]);
export const suppressions = sqliteTable("crm_suppressions", { id: text("id").primaryKey(), ...tenantColumns(), contactId: text("contact_id").notNull(), channel: text("channel"), reason: text("reason").notNull(), active: integer("active", { mode: "boolean" }).notNull().default(true), occurredAt: integer("occurred_at", { mode: "timestamp_ms" }).notNull() }, (t) => [index("crm_suppression_contact_idx").on(t.tenantId, t.contactId, t.channel, t.active)]);
export const journeys = sqliteTable("crm_journeys", { id: text("id").primaryKey(), ...tenantColumns(), key: text("key").notNull(), versionLabel: text("version_label").notNull(), definitionJson: text("definition_json").notNull(), active: integer("active", { mode: "boolean" }).notNull().default(false) }, (t) => [uniqueIndex("crm_journey_version_uq").on(t.tenantId, t.key, t.versionLabel)]);
export const journeySteps = sqliteTable("crm_journey_steps", { id: text("id").primaryKey(), ...tenantColumns(), journeyId: text("journey_id").notNull(), sequence: integer("sequence").notNull(), purpose: text("purpose").notNull(), channel: text("channel").notNull(), delaySeconds: integer("delay_seconds").notNull() }, (t) => [uniqueIndex("crm_journey_step_uq").on(t.tenantId, t.journeyId, t.sequence)]);
export const scheduledTouches = sqliteTable("crm_scheduled_touches", { id: text("id").primaryKey(), ...tenantColumns(), leadId: text("lead_id").notNull(), contactId: text("contact_id").notNull(), journeyStepId: text("journey_step_id"), purpose: text("purpose").notNull().default("legacy_unspecified"), requestedChannel: text("requested_channel").notNull().default("unknown"), dueAt: integer("due_at", { mode: "timestamp_ms" }).notNull(), status: text("status").notNull().default("planned"), gateKey: text("gate_key").notNull() }, (t) => [uniqueIndex("crm_scheduled_gate_uq").on(t.tenantId, t.gateKey), index("crm_touches_due_idx").on(t.tenantId, t.status, t.dueAt)]);
export const messageAttempts = sqliteTable("crm_message_attempts", { id: text("id").primaryKey(), ...tenantColumns(), touchId: text("touch_id"), contactId: text("contact_id").notNull(), channel: text("channel").notNull(), requestedChannel: text("requested_channel").notNull().default("unknown"), purpose: text("purpose").notNull(), templateVersionId: text("template_version_id"), acceptedContentHash: text("accepted_content_hash"), provider: text("provider"), providerMessageId: text("provider_message_id"), status: text("status").notNull().default("planned"), acceptedAt: integer("accepted_at", { mode: "timestamp_ms" }) }, (t) => [uniqueIndex("crm_message_provider_uq").on(t.tenantId, t.provider, t.providerMessageId), index("crm_messages_contact_idx").on(t.tenantId, t.contactId, t.acceptedAt)]);
export const messageEvents = sqliteTable("crm_message_events", { id: text("id").primaryKey(), ...tenantColumns(), messageAttemptId: text("message_attempt_id").notNull(), providerEventId: text("provider_event_id").notNull(), type: text("type").notNull(), occurredAt: integer("occurred_at", { mode: "timestamp_ms" }).notNull(), payloadCiphertext: text("payload_ciphertext") }, (t) => [uniqueIndex("crm_message_event_uq").on(t.tenantId, t.providerEventId)]);
export const contactDispatchGates = sqliteTable("crm_contact_dispatch_gates", { id: text("id").primaryKey(), ...tenantColumns(), contactId: text("contact_id").notNull(), activeTouchId: text("active_touch_id"), reservedUntil: integer("reserved_until", { mode: "timestamp_ms" }), lastAcceptedAt: integer("last_accepted_at", { mode: "timestamp_ms" }), version: integer("version").notNull().default(1) }, (t) => [uniqueIndex("crm_dispatch_gate_contact_uq").on(t.tenantId, t.contactId)]);
export const channelCosts = sqliteTable("crm_channel_costs", { id: text("id").primaryKey(), ...tenantColumns(), messageAttemptId: text("message_attempt_id"), channel: text("channel").notNull(), amountMinor: integer("amount_minor").notNull(), currency: text("currency").notNull(), estimated: integer("estimated", { mode: "boolean" }).notNull().default(true) });
