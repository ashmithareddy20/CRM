import { index, integer, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
import { tenantColumns } from "./common";
export const lifecycleStages = sqliteTable("crm_lifecycle_stages", { id: text("id").primaryKey(), ...tenantColumns(), key: text("key").notNull(), label: text("label").notNull(), terminal: integer("terminal", { mode: "boolean" }).notNull().default(false) }, (t) => [uniqueIndex("crm_lifecycle_stages_key_uq").on(t.tenantId, t.key)]);
export const lifecycleReasons = sqliteTable("crm_lifecycle_reasons", { id: text("id").primaryKey(), ...tenantColumns(), stageId: text("stage_id"), key: text("key").notNull(), label: text("label").notNull(), requiresEvidence: integer("requires_evidence", { mode: "boolean" }).notNull().default(false) }, (t) => [uniqueIndex("crm_lifecycle_reasons_key_uq").on(t.tenantId, t.key)]);
export const allowedTransitions = sqliteTable("crm_allowed_transitions", { id: text("id").primaryKey(), ...tenantColumns(), fromStageId: text("from_stage_id").notNull(), toStageId: text("to_stage_id").notNull(), policyJson: text("policy_json").notNull() }, (t) => [uniqueIndex("crm_allowed_transition_uq").on(t.tenantId, t.fromStageId, t.toStageId)]);
export const lifecycleTransitions = sqliteTable("crm_lifecycle_transitions", { id: text("id").primaryKey(), ...tenantColumns(), leadId: text("lead_id").notNull(), fromStage: text("from_stage").notNull(), toStage: text("to_stage").notNull(), reasonId: text("reason_id"), commandId: text("command_id").notNull(), expectedVersion: integer("expected_version").notNull(), occurredAt: integer("occurred_at", { mode: "timestamp_ms" }).notNull(), evidenceId: text("evidence_id") }, (t) => [uniqueIndex("crm_transition_command_uq").on(t.tenantId, t.commandId), index("crm_transitions_lead_idx").on(t.tenantId, t.leadId, t.occurredAt)]);
export const nextActionCommitments = sqliteTable("crm_next_action_commitments", { id: text("id").primaryKey(), ...tenantColumns(), leadId: text("lead_id").notNull(), ownerMembershipId: text("owner_membership_id").notNull(), dueAt: integer("due_at", { mode: "timestamp_ms" }).notNull(), action: text("action").notNull(), status: text("status").notNull().default("open") }, (t) => [index("crm_next_actions_due_idx").on(t.tenantId, t.ownerMembershipId, t.dueAt)]);
export const leadOutcomes = sqliteTable("crm_lead_outcomes", { id: text("id").primaryKey(), ...tenantColumns(), leadId: text("lead_id").notNull(), outcome: text("outcome").notNull(), reasonId: text("reason_id"), diagnosisId: text("diagnosis_id"), occurredAt: integer("occurred_at", { mode: "timestamp_ms" }).notNull() }, (t) => [uniqueIndex("crm_outcome_lead_uq").on(t.tenantId, t.leadId)]);
