import { index, integer, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
import { tenantColumns } from "./common";
export const contacts = sqliteTable("crm_contacts", { id: text("id").primaryKey(), ...tenantColumns(), nameCiphertext: text("name_ciphertext"), phoneCiphertext: text("phone_ciphertext"), phoneBlindIndex: text("phone_blind_index"), emailCiphertext: text("email_ciphertext"), emailBlindIndex: text("email_blind_index"), status: text("status").notNull().default("active") }, (t) => [uniqueIndex("crm_contacts_tenant_id_uq").on(t.tenantId, t.id), index("crm_contacts_phone_idx").on(t.tenantId, t.phoneBlindIndex), index("crm_contacts_email_idx").on(t.tenantId, t.emailBlindIndex)]);
export const sourceTaxonomy = sqliteTable("crm_source_taxonomy", { id: text("id").primaryKey(), ...tenantColumns(), key: text("key").notNull(), label: text("label").notNull(), active: integer("active", { mode: "boolean" }).notNull().default(true) }, (t) => [uniqueIndex("crm_source_taxonomy_key_uq").on(t.tenantId, t.key)]);
export const campaigns = sqliteTable("crm_campaigns", { id: text("id").primaryKey(), ...tenantColumns(), sourceId: text("source_id").notNull(), provider: text("provider").notNull(), externalId: text("external_id"), name: text("name").notNull(), startsAt: integer("starts_at", { mode: "timestamp_ms" }), endsAt: integer("ends_at", { mode: "timestamp_ms" }) }, (t) => [uniqueIndex("crm_campaigns_external_uq").on(t.tenantId, t.provider, t.externalId), uniqueIndex("crm_campaigns_tenant_id_uq").on(t.tenantId, t.id)]);
export const adSets = sqliteTable("crm_ad_sets", { id: text("id").primaryKey(), ...tenantColumns(), campaignId: text("campaign_id").notNull(), provider: text("provider").notNull(), externalId: text("external_id"), name: text("name").notNull() }, (t) => [uniqueIndex("crm_ad_sets_external_uq").on(t.tenantId, t.provider, t.externalId)]);
export const creatives = sqliteTable("crm_creatives", { id: text("id").primaryKey(), ...tenantColumns(), adSetId: text("ad_set_id").notNull(), provider: text("provider").notNull(), externalId: text("external_id"), name: text("name").notNull() }, (t) => [uniqueIndex("crm_creatives_external_uq").on(t.tenantId, t.provider, t.externalId)]);
export const leadForms = sqliteTable("crm_lead_forms", { id: text("id").primaryKey(), ...tenantColumns(), campaignId: text("campaign_id"), provider: text("provider").notNull(), externalId: text("external_id").notNull(), name: text("name").notNull() }, (t) => [uniqueIndex("crm_lead_forms_external_uq").on(t.tenantId, t.provider, t.externalId)]);
export const leadEpisodes = sqliteTable("crm_lead_episodes", { id: text("id").primaryKey(), ...tenantColumns(), contactId: text("contact_id").notNull(), sourceId: text("source_id").notNull(), campaignId: text("campaign_id"), branchId: text("branch_id"), assignedMembershipId: text("assigned_membership_id"), lifecycleStage: text("lifecycle_stage").notNull().default("received"), receivedAt: integer("received_at", { mode: "timestamp_ms" }).notNull(), generatedAt: integer("generated_at", { mode: "timestamp_ms" }), archivedReason: text("archived_reason") /* deprecated compatibility rationale; archivedAt is canonical archive state */ }, (t) => [uniqueIndex("crm_leads_tenant_id_uq").on(t.tenantId, t.id), index("crm_leads_scope_idx").on(t.tenantId, t.lifecycleStage, t.assignedMembershipId, t.receivedAt), index("crm_leads_contact_idx").on(t.tenantId, t.contactId)]);
export const acquisitionTouches = sqliteTable("crm_acquisition_touches", { id: text("id").primaryKey(), ...tenantColumns(), leadId: text("lead_id").notNull(), campaignId: text("campaign_id"), formId: text("form_id"), occurredAt: integer("occurred_at", { mode: "timestamp_ms" }).notNull(), utmJson: text("utm_json"), referrer: text("referrer") }, (t) => [index("crm_acquisition_lead_idx").on(t.tenantId, t.leadId, t.occurredAt)]);
export const externalSubmissions = sqliteTable("crm_external_submissions", { id: text("id").primaryKey(), ...tenantColumns(), leadId: text("lead_id"), provider: text("provider").notNull(), externalId: text("external_id").notNull(), payloadCiphertext: text("payload_ciphertext"), receivedAt: integer("received_at", { mode: "timestamp_ms" }).notNull() }, (t) => [uniqueIndex("crm_external_submission_uq").on(t.tenantId, t.provider, t.externalId)]);
export const dedupCandidates = sqliteTable("crm_dedup_candidates", { id: text("id").primaryKey(), ...tenantColumns(), leadId: text("lead_id").notNull(), candidateLeadId: text("candidate_lead_id").notNull(), reason: text("reason").notNull(), status: text("status").notNull().default("open") }, (t) => [uniqueIndex("crm_dedup_pair_uq").on(t.tenantId, t.leadId, t.candidateLeadId)]);
export const leadMergeLinks = sqliteTable("crm_lead_merge_links", { id: text("id").primaryKey(), ...tenantColumns(), canonicalLeadId: text("canonical_lead_id").notNull(), mergedLeadId: text("merged_lead_id").notNull(), reason: text("reason").notNull() }, (t) => [uniqueIndex("crm_merge_merged_uq").on(t.tenantId, t.mergedLeadId)]);
export const assignmentRules = sqliteTable("crm_assignment_rules", { id: text("id").primaryKey(), ...tenantColumns(), name: text("name").notNull(), priority: integer("priority").notNull(), definitionJson: text("definition_json").notNull(), active: integer("active", { mode: "boolean" }).notNull().default(true) });
export const assignmentHistory = sqliteTable("crm_assignment_history", { id: text("id").primaryKey(), ...tenantColumns(), leadId: text("lead_id").notNull(), fromMembershipId: text("from_membership_id"), toMembershipId: text("to_membership_id"), ruleId: text("rule_id"), reason: text("reason").notNull(), assignedAt: integer("assigned_at", { mode: "timestamp_ms" }).notNull() }, (t) => [index("crm_assignment_history_idx").on(t.tenantId, t.leadId, t.assignedAt)]);
