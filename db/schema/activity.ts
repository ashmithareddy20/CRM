import { index, integer, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
import { tenantColumns } from "./common";
export const callAttempts = sqliteTable("crm_call_attempts", { id: text("id").primaryKey(), ...tenantColumns(), leadId: text("lead_id").notNull(), membershipId: text("membership_id"), provider: text("provider"), externalId: text("external_id"), direction: text("direction").notNull(), disposition: text("disposition").notNull().default("pending"), dialedAt: integer("dialed_at", { mode: "timestamp_ms" }), connectedAt: integer("connected_at", { mode: "timestamp_ms" }), endedAt: integer("ended_at", { mode: "timestamp_ms" }), legacyEvidence: integer("legacy_evidence", { mode: "boolean" }).notNull().default(false) }, (t) => [uniqueIndex("crm_call_provider_event_uq").on(t.tenantId, t.provider, t.externalId), index("crm_calls_lead_idx").on(t.tenantId, t.leadId, t.dialedAt)]);
export const callRemarks = sqliteTable("crm_call_remarks", { id: text("id").primaryKey(), ...tenantColumns(), callAttemptId: text("call_attempt_id").notNull(), patientStatementCiphertext: text("patient_statement_ciphertext"), agentExplanationCiphertext: text("agent_explanation_ciphertext"), objection: text("objection"), nextAction: text("next_action"), nextActionDueAt: integer("next_action_due_at", { mode: "timestamp_ms" }), notApplicableReason: text("not_applicable_reason") }, (t) => [uniqueIndex("crm_call_remarks_attempt_uq").on(t.tenantId, t.callAttemptId)]);
export const activityNotes = sqliteTable("crm_notes", { id: text("id").primaryKey(), ...tenantColumns(), leadId: text("lead_id").notNull(), contentCiphertext: text("content_ciphertext").notNull(), immutableAt: integer("immutable_at", { mode: "timestamp_ms" }).notNull() }, (t) => [index("crm_notes_lead_idx").on(t.tenantId, t.leadId, t.immutableAt)]);
export const noteAmendments = sqliteTable("crm_note_amendments", { id: text("id").primaryKey(), ...tenantColumns(), noteId: text("note_id").notNull(), contentCiphertext: text("content_ciphertext").notNull(), reason: text("reason").notNull() });
export const evidenceAssets = sqliteTable("crm_evidence_assets", { id: text("id").primaryKey(), ...tenantColumns(), leadId: text("lead_id"), objectKey: text("object_key").notNull(), sha256: text("sha256").notNull(), mediaType: text("media_type").notNull(), classification: text("classification").notNull(), status: text("status").notNull().default("pending") }, (t) => [uniqueIndex("crm_evidence_object_uq").on(t.tenantId, t.objectKey), index("crm_evidence_lead_idx").on(t.tenantId, t.leadId)]);
export const contactCapabilities = sqliteTable("crm_contact_capabilities", { id: text("id").primaryKey(), ...tenantColumns(), contactId: text("contact_id").notNull(), channel: text("channel").notNull(), capability: text("capability").notNull(), observedAt: integer("observed_at", { mode: "timestamp_ms" }).notNull(), source: text("source").notNull() }, (t) => [uniqueIndex("crm_capability_uq").on(t.tenantId, t.contactId, t.channel, t.capability, t.observedAt)]);
