import { index, integer, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
import { tenantColumns } from "./common";
export const reportingFacts = sqliteTable("crm_reporting_facts", { id: text("id").primaryKey(), ...tenantColumns(), sourceEventId: text("source_event_id").notNull(), factType: text("fact_type").notNull(), occurredAt: integer("occurred_at", { mode: "timestamp_ms" }).notNull(), dimensionsJson: text("dimensions_json").notNull(), valueMinor: integer("value_minor") }, (t) => [uniqueIndex("crm_reporting_fact_source_uq").on(t.tenantId, t.sourceEventId), index("crm_reporting_fact_idx").on(t.tenantId, t.factType, t.occurredAt), index("crm_reporting_fact_cursor_idx").on(t.tenantId, t.occurredAt, t.id)]);
export const dailyAggregates = sqliteTable("crm_daily_aggregates", { id: text("id").primaryKey(), ...tenantColumns(), metricKey: text("metric_key").notNull(), localDate: text("local_date").notNull(), dimensionsHash: text("dimensions_hash").notNull(), valueJson: text("value_json").notNull(), watermark: text("watermark").notNull() }, (t) => [uniqueIndex("crm_daily_aggregate_uq").on(t.tenantId, t.metricKey, t.localDate, t.dimensionsHash)]);
export const campaignSpend = sqliteTable("crm_campaign_spend", { id: text("id").primaryKey(), ...tenantColumns(), campaignId: text("campaign_id").notNull(), amountMinor: integer("amount_minor").notNull(), currency: text("currency").notNull(), occurredAt: integer("occurred_at", { mode: "timestamp_ms" }).notNull() });
export const reportRuns = sqliteTable("crm_report_runs", { id: text("id").primaryKey(), ...tenantColumns(), type: text("type").notNull(), filtersJson: text("filters_json").notNull(), status: text("status").notNull(), completedAt: integer("completed_at", { mode: "timestamp_ms" }) });
export const kpiDefinitions = sqliteTable("crm_kpi_definitions", { id: text("id").primaryKey(), ...tenantColumns(), key: text("key").notNull(), versionLabel: text("version_label").notNull(), definitionJson: text("definition_json").notNull() }, (t) => [uniqueIndex("crm_kpi_version_uq").on(t.tenantId, t.key, t.versionLabel)]);
