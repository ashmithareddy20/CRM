import { sql } from "drizzle-orm";
import { index, integer, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
import { tenantColumns } from "./common";
export const diagnosisVersions = sqliteTable("crm_diagnosis_versions", { id: text("id").primaryKey(), ...tenantColumns(), key: text("key").notNull(), versionLabel: text("version_label").notNull(), definitionJson: text("definition_json").notNull() }, (t) => [uniqueIndex("crm_diagnosis_version_uq").on(t.tenantId, t.key, t.versionLabel)]);
export const leadDiagnoses = sqliteTable("crm_lead_diagnoses", { id: text("id").primaryKey(), ...tenantColumns(), leadId: text("lead_id").notNull(), versionId: text("version_id").notNull(), primaryReason: text("primary_reason").notNull(), secondaryReason: text("secondary_reason"), evidenceId: text("evidence_id").notNull(), recoverability: text("recoverability").notNull(), reviewAt: integer("review_at", { mode: "timestamp_ms" }).notNull(), status: text("status").notNull().default("active") }, (t) => [index("crm_diagnoses_lead_idx").on(t.tenantId, t.leadId, t.status), uniqueIndex("crm_lead_active_diagnosis_uq").on(t.tenantId, t.leadId).where(sql`${t.status} = 'active'`)]);
export const recoveryCampaigns = sqliteTable("crm_recovery_campaigns", { id: text("id").primaryKey(), ...tenantColumns(), name: text("name").notNull(), status: text("status").notNull().default("draft"), definitionJson: text("definition_json").notNull() });
export const recoveryEnrollments = sqliteTable("crm_recovery_enrollments", { id: text("id").primaryKey(), ...tenantColumns(), leadId: text("lead_id").notNull(), diagnosisId: text("diagnosis_id").notNull(), campaignId: text("campaign_id").notNull(), eligibleAt: integer("eligible_at", { mode: "timestamp_ms" }).notNull(), reactivationAt: integer("reactivation_at", { mode: "timestamp_ms" }), reasonSnapshot: text("reason_snapshot"), status: text("status").notNull().default("planned") }, (t) => [uniqueIndex("crm_recovery_active_lead_uq").on(t.tenantId, t.leadId).where(sql`${t.status} IN ('planned', 'active', 'paused')`), index("crm_recovery_due_idx").on(t.tenantId, t.status, t.reactivationAt)]);
export const managementFindings = sqliteTable("crm_management_findings", { id: text("id").primaryKey(), ...tenantColumns(), diagnosisId: text("diagnosis_id"), finding: text("finding").notNull(), evidenceId: text("evidence_id"), ownerMembershipId: text("owner_membership_id"), reviewAt: integer("review_at", { mode: "timestamp_ms" }) });
